<section class="p-5">
    <div class="row ">
        {{#each footer}}
        <div class="col-2">
            <div class="nav flex-column">
                {{#each this}}
                <a href="link" class="nav-link {{#if @last}} disabled {{/if}}"> {{ text}} </a>
                {{/each}}
            </div>
        </div>
        {{/each}}
    </div>
</section>